---
label: contact
hide_body: true
fields:
- name: links
  type: field_group_list
  fields:
  - name: url
    type: text
    config:
      required: true
    label: url
  - name: hover
    type: text
    config:
      required: true
    label: hover
  - name: icon
    type: file
    config:
      maxSize: 64
    label: icon
  config:
    min: 
    max: 
    labelField: hover
  label: contact links
pages:
- content/about/contact.json
