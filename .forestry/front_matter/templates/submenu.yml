---
label: Submenu
hide_body: true
fields:
- name: slug
  type: text
  config:
    required: true
  label: Slug
- name: items
  type: blocks
  label: Items
  template_types:
  - submenu
  - link
  config:
    min: 
    max: 
