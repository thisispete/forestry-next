---
label: Nav
hide_body: true
fields:
- type: field_group_list
  name: nav
  label: nav
  fields:
  - name: type
    type: text
    config:
      required: true
    label: type
    default: submenu
  config:
    labelField: slug
pages:
- content/nav.json
